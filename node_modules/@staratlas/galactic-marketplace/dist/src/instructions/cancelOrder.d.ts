import { Account } from '@solana/spl-token';
import { Connection, PublicKey } from '@solana/web3.js';
import { InstructionReturn } from '@staratlas/data-source';
import { GalacticMarketplaceIDLProgram } from '../constants';
import { OpenOrdersCounter, Order } from '../state';
/**
 * Cancels Order with Solana connection
 * Takes a connection to fetch necessary accounts - it is preferred to use `cancelAccount`
 * if account data can be provided without an RPC connection.
 * @param connection - Connection
 * @param orderInitializer - Order Initializer Key
 * @param orderAccount - Order Account
 * @param program - Galactic Marketplace Program
 * @param transferHookProgramId - The optional transfer hook program id
 * @returns InstructionReturn
 */
export declare function cancelOrderWithConnection(connection: Connection, orderInitializer: PublicKey, orderAccount: Order, program: GalacticMarketplaceIDLProgram, transferHookProgramId?: PublicKey): Promise<InstructionReturn[]>;
/**
 * Cancels Order
 * @param program -Galactic Marketplace Program
 * @param orderAccount - Order Account
 * @param orderInitializer - Order Initializer Key
 * @param initializerDepositTokenAccount - Deposit Token Account
 * @param openOrdersCounter - OpenOrdersCounter Account/Key
 * @param transferHookProgramId - The optional transfer hook program id
 * @returns InstructionReturn
 */
export declare function cancelOrder(program: GalacticMarketplaceIDLProgram, orderAccount: Order, orderInitializer: PublicKey, initializerDepositTokenAccount: {
    account: Account;
} | {
    key: PublicKey;
}, openOrdersCounter: {
    openOrdersCounter: OpenOrdersCounter;
} | {
    key: PublicKey;
}, transferHookProgramId?: PublicKey): InstructionReturn;
/**
 * Cancels Order (without requiring an Order account)
 * @param program -Galactic Marketplace Program
 * @param orderAccount - Order Account address
 * @param orderSide - the type of order (either buy or sell)
 * @param depositMint - the mint of the token that was deposited when the order was made
 * @param orderInitializer - Order Initializer Key
 * @param initializerDepositTokenAccount - Deposit Token Account
 * @param openOrdersCounter - OpenOrdersCounter Account/Key
 * @param transferHookProgramId - The optional transfer hook program id
 * @returns InstructionReturn
 */
export declare function cancelOrderBareBones(program: GalacticMarketplaceIDLProgram, orderAccount: PublicKey, orderSide: 'BuySide' | 'SellSide', depositMint: PublicKey, orderInitializer: PublicKey, initializerDepositTokenAccount: PublicKey, openOrdersCounter: PublicKey, transferHookProgramId?: PublicKey): InstructionReturn;
//# sourceMappingURL=cancelOrder.d.ts.map