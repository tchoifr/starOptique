import { KeyedAccountInfo, PublicKey } from '@solana/web3.js';
import { Account, DecodedAccountData } from '@staratlas/data-source';
import { GalacticMarketplaceIDL } from '..';
import { GalacticMarketplaceIDLProgram, RegisteredCurrencyAccount } from '../constants';
/**
 * Check if two instances of `RegisteredCurrencyAccount` are equal
 * @param registeredCurrencyData1 - the first instance
 * @param registeredCurrencyData2 - the second instance
 * @returns boolean
 */
export declare function registeredCurrencyDataEquals(registeredCurrencyData1: RegisteredCurrencyAccount, registeredCurrencyData2: RegisteredCurrencyAccount): boolean;
export declare const ROYALTY_TIER_SIZE: number;
export declare const VEC_ROYALTY_TIER_SIZE: number;
export declare class RegisteredCurrency implements Account {
    private _data;
    private _key;
    static readonly ACCOUNT_NAME: NonNullable<GalacticMarketplaceIDL['accounts']>[number]['name'];
    static readonly MIN_DATA_SIZE: number;
    constructor(_data: RegisteredCurrencyAccount, _key: PublicKey);
    get data(): Readonly<RegisteredCurrencyAccount>;
    get key(): PublicKey;
    /**
     * Find the `RegisteredCurrency` account address
     * @param program - GalacticMarketplace Program
     * @param currencyMint - the current mint
     * @returns PDA and bump
     */
    static findAddress(program: GalacticMarketplaceIDLProgram, currencyMint: PublicKey): [PublicKey, number];
    static decodeData(account: KeyedAccountInfo, program: GalacticMarketplaceIDLProgram): DecodedAccountData<RegisteredCurrency>;
}
//# sourceMappingURL=registeredCurrency.d.ts.map