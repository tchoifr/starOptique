import { KeyedAccountInfo, PublicKey } from '@solana/web3.js';
import { Account, DecodedAccountData } from '@staratlas/data-source';
import { GalacticMarketplaceIDL } from '..';
import { GalacticMarketplaceIDLProgram, MarketVarsAccount } from '../constants';
/**
 * Check if two instances of `MarketVarsAccount` are equal
 * @param marketVarsData1 - the first instance
 * @param marketVarsData2 - the second instance
 * @returns boolean
 */
export declare function marketVarsDataEquals(marketVarsData1: MarketVarsAccount, marketVarsData2: MarketVarsAccount): boolean;
export declare class MarketVars implements Account {
    private _data;
    private _key;
    static readonly ACCOUNT_NAME: NonNullable<GalacticMarketplaceIDL['accounts']>[number]['name'];
    static readonly MIN_DATA_SIZE: number;
    constructor(_data: MarketVarsAccount, _key: PublicKey);
    get data(): Readonly<MarketVarsAccount>;
    get key(): PublicKey;
    /**
     * Find the `MarketVars` account address
     * @param program - GalacticMarketplace Program
     * @returns PDA and bump
     */
    static findAddress(program: GalacticMarketplaceIDLProgram): [PublicKey, number];
    static decodeData(account: KeyedAccountInfo, program: GalacticMarketplaceIDLProgram): DecodedAccountData<MarketVars>;
}
//# sourceMappingURL=marketVars.d.ts.map