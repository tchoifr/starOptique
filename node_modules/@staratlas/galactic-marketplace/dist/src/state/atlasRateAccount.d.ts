import { KeyedAccountInfo, PublicKey } from '@solana/web3.js';
import { Account, DecodedAccountData } from '@staratlas/data-source';
import { AtlasRateAccount, GalacticMarketplaceIDL, GalacticMarketplaceIDLProgram } from '../constants';
export declare const ATLAS_RATE_ACCOUNT_MIN_DATA_SIZE: number;
export declare class AtlasRate implements Account {
    private _data;
    private _key;
    static readonly ACCOUNT_NAME: NonNullable<GalacticMarketplaceIDL['accounts']>[number]['name'];
    static readonly MIN_DATA_SIZE: number;
    constructor(_data: AtlasRateAccount, _key: PublicKey);
    get data(): Readonly<AtlasRateAccount>;
    get key(): Readonly<PublicKey>;
    /**
     * Find the AtlasRate account address
     * @param program - Marketplace program
     * @returns PDA for Atlas Rate Account
     */
    static findAddress(program: GalacticMarketplaceIDLProgram): [PublicKey, number];
    static decodeData(account: KeyedAccountInfo, program: GalacticMarketplaceIDLProgram): DecodedAccountData<AtlasRate>;
}
//# sourceMappingURL=atlasRateAccount.d.ts.map