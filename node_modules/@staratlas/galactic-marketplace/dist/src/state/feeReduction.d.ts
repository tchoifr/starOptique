import { KeyedAccountInfo, PublicKey } from '@solana/web3.js';
import { Account, DecodedAccountData } from '@staratlas/data-source';
import { FeeReductionAccount, GalacticMarketplaceIDL, GalacticMarketplaceIDLProgram } from '../constants';
export declare const FEE_REDUCTION_MIN_DATA_SIZE: number;
export declare class FeeReduction implements Account {
    private _data;
    private _key;
    static readonly ACCOUNT_NAME: NonNullable<GalacticMarketplaceIDL['accounts']>[number]['name'];
    static readonly MIN_DATA_SIZE: number;
    constructor(_data: FeeReductionAccount, _key: PublicKey);
    get data(): Readonly<FeeReductionAccount>;
    get key(): Readonly<PublicKey>;
    /**
     * Find the FeeReduction account address
     * @param targetAccount - Account key
     * @param program - Marketplace program
     * @returns PDA for Fee Reduction account
     */
    static findAddress(targetAccount: PublicKey, program: GalacticMarketplaceIDLProgram): [PublicKey, number];
    static decodeData(account: KeyedAccountInfo, program: GalacticMarketplaceIDLProgram): DecodedAccountData<FeeReduction>;
}
//# sourceMappingURL=feeReduction.d.ts.map