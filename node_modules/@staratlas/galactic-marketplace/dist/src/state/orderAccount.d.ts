import { KeyedAccountInfo, PublicKey } from '@solana/web3.js';
import { Account, DecodedAccountData } from '@staratlas/data-source';
import { GalacticMarketplaceIDL } from '..';
import { GalacticMarketplaceIDLProgram, OrderAccount } from '../constants';
/**
 * Check if two instances of `OrderAccount` are equal
 * @param orderData1 - the first instance
 * @param orderData2 - the second instance
 * @returns boolean
 */
export declare function orderDataEquals(orderData1: OrderAccount, orderData2: OrderAccount): boolean;
export declare class Order implements Account {
    private _data;
    private _key;
    static readonly ACCOUNT_NAME: NonNullable<GalacticMarketplaceIDL['accounts']>[number]['name'];
    static readonly MIN_DATA_SIZE: number;
    constructor(_data: OrderAccount, _key: PublicKey);
    /**
     * Find the `OrderVault` account address
     * @param program - GalacticMarketplace Program
     * @param orderInitializer - the order initializer
     * @param depositMint - the deposit mint
     * @returns PDA and bump
     */
    static getOrderVaultAddress(program: GalacticMarketplaceIDLProgram, orderInitializer: PublicKey, depositMint: PublicKey): [PublicKey, number];
    /**
     * Find the `OrderVaultAuthority` account address
     * @param program - GalacticMarketplace Program
     * @param orderInitializer - the order initializer
     * @returns PDA and bump
     */
    static getOrderVaultAuthorityAddress(program: GalacticMarketplaceIDLProgram, orderInitializer: PublicKey): [PublicKey, number];
    get data(): Readonly<OrderAccount>;
    get key(): PublicKey;
    static decodeData(account: KeyedAccountInfo, program: GalacticMarketplaceIDLProgram): DecodedAccountData<Order>;
}
//# sourceMappingURL=orderAccount.d.ts.map