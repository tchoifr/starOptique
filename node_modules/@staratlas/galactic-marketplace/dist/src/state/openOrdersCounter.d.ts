import { KeyedAccountInfo, PublicKey } from '@solana/web3.js';
import { Account, DecodedAccountData } from '@staratlas/data-source';
import { GalacticMarketplaceIDL } from '..';
import { GalacticMarketplaceIDLProgram, OpenOrdersCounterAccount } from '../constants';
/**
 * Check if two instances of `OpenOrdersCounterAccount` are equal
 * @param openOrdersCounterData1 - the first instance
 * @param openOrdersCounterData2 - the second instance
 * @returns boolean
 */
export declare function openOrdersCounterDataEquals(openOrdersCounterData1: OpenOrdersCounterAccount, openOrdersCounterData2: OpenOrdersCounterAccount): boolean;
export declare class OpenOrdersCounter implements Account {
    private _data;
    private _key;
    static readonly ACCOUNT_NAME: NonNullable<GalacticMarketplaceIDL['accounts']>[number]['name'];
    static readonly MIN_DATA_SIZE: number;
    constructor(_data: OpenOrdersCounterAccount, _key: PublicKey);
    get data(): Readonly<OpenOrdersCounterAccount>;
    get key(): PublicKey;
    /**
     * Find the `OpenOrdersCounter` account address
     * @param program - GalacticMarketplace Program
     * @param playerPubkey - the player address
     * @param depositMint - the deposit mint address
     * @returns PDA and bump
     */
    static findAddress(program: GalacticMarketplaceIDLProgram, playerPubkey: PublicKey, depositMint: PublicKey): [PublicKey, number];
    static decodeData(account: KeyedAccountInfo, program: GalacticMarketplaceIDLProgram): DecodedAccountData<OpenOrdersCounter>;
}
//# sourceMappingURL=openOrdersCounter.d.ts.map